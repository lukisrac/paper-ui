import { CustomProjectConfig } from "lost-pixel";

export const config: CustomProjectConfig = {
    storybookShots: {
        storybookUrl: "./storybook-static",
    },

    lostPixelProjectId: "clx5zl20n54csh7topebm5l90",
    apiKey: process.env.LOST_PIXEL_API_KEY,
};
